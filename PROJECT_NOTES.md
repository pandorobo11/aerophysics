# aerophysics プロジェクトメモ

## 目的

`aerophysics` は、流体の状態と局所的な空力現象を、解析式および半経験式によって計算する Python 科学計算パッケージとする。

個別の航空機形状を設計・解析するツールではなく、空力設計で再利用できる基礎物理モデルを、検証可能な API として提供する。

## パッケージ名

- パッケージ名: `aerophysics`
- 名称の意図: 単なる便利関数集ではなく、標準大気、圧縮性流れ、衝撃波、境界層、空力加熱などの物理モデルを体系的に扱う。
- `aerocalc` は内容を直感的に表す一方、同名かつ類似用途の PyPI パッケージが既に存在する。
- `aerotools` は拡張性が高いが、用途が広すぎてパッケージの専門性が伝わりにくい。

## 対象範囲

### 1. 標準大気・気体物性

- 標準大気モデル
- 温度、圧力、密度、音速
- Sutherland 則による粘性係数
- 比熱、比熱比、気体定数、Prandtl 数
- 必要に応じた空気以外の完全気体モデル

### 2. 飛行条件と無次元数

- Mach 数、速度、音速の相互変換
- 動圧および有効動圧
- Reynolds 数と単位長さ当たり Reynolds 数
- 全温、全圧、静温、静圧
- 高度と速度条件を統合する `FlightCondition` API

### 3. 等エントロピー圧縮性流れ

- 静的状態量と全状態量の関係
- 面積–Mach 数関係
- チョーク条件
- 与えられた状態量比から Mach 数を求める逆計算

### 4. 衝撃波・膨張波

- 垂直衝撃波
- 斜め衝撃波
- theta–beta–Mach 関係
- 弱解と強解
- Prandtl–Meyer 膨張波
- 超音速ピトー管関係

### 5. ダクト流れ・ノズル流れ

- 収縮・拡大ノズル
- Fanno 流れ
- Rayleigh 流れ
- 臨界状態と流量

### 6. 境界層と壁面摩擦

- 層流および乱流平板境界層
- 境界層厚さ、排除厚さ、運動量厚さ
- 局所および平均摩擦係数
- 平板の摩擦抗力
- 圧縮性補正
- 表面粗さ、遷移位置、剥離の簡易推定

### 7. 空力加熱

- 断熱壁温度と回復温度
- 壁面熱流束
- 平板およびよどみ点の加熱率
- Stanton 数と熱境界層

### 8. 高温気体モデル

- 温度依存の比熱と比熱比
- 実在気体補正
- 極超音速流れ向けの簡易 Newtonian 理論
- 化学反応・熱的非平衡モデルは初期版の対象外とする

### 9. 共通基盤

- SI 単位を基本とした一貫した入出力
- 必要な航空慣用単位の変換
- NumPy 配列入力への対応
- 順問題と逆問題
- 適用範囲外入力に対する明確な例外または警告
- 使用した式、モデル名、出典、仮定、適用範囲の文書化

## 対象外

初期パッケージでは、特定の翼・機体形状に依存する設計計算を扱わない。

- 翼型生成および翼型解析
- 揚力線理論、Vortex Lattice Method
- 有限翼の揚力、誘導抗力
- 機体全体の drag buildup
- 干渉抗力、揚抗比、航空機性能
- 形状最適化

ただし、局所摩擦係数、平板摩擦抗力、衝撃波による圧力損失など、基礎物理モデルから直接求まる量は対象に含める。

将来これらの機体設計機能が必要になった場合は、`aerophysics` を基盤として利用する別パッケージに分離する。

## 想定パッケージ構成

```text
aerophysics/
├── atmosphere.py
├── gas.py
├── flight.py
├── isentropic.py
├── shocks.py
├── expansion.py
├── nozzle.py
├── duct.py
├── boundary_layer.py
├── heating.py
└── units.py
```

実装開始時には `src/aerophysics/` 配置を採用し、各物理領域を独立したモジュールとして構成する。

## 初期リリースの優先順位

1. 気体物性と標準大気
2. `FlightCondition` と無次元数
3. 等エントロピー圧縮性流れ
4. 垂直・斜め衝撃波と膨張波
5. 層流・乱流平板境界層
6. ノズル、Fanno 流れ、Rayleigh 流れ
7. 空力加熱と高温気体モデル

最初の実用版では、1〜5を中心に API、数値検証、出典付きドキュメントを整備する。

## 設計方針

- 計算式の正しさと追跡可能性を優先する。
- 暗黙の単位変換を避け、単位規約を明示する。
- スカラーと配列で一貫した結果を返す。
- 適用範囲、完全気体などの仮定、相関式の限界を API ドキュメントに記載する。
- 代表的な教科書値、解析解、既知の基準データを使ってテストする。
- 公開 API は小さく保ち、内部実装と分離する。

## 0.1 の実装状況

- Python パッケージ、開発ツール、3 OS の CI を構成済み。
- 完全気体、空気輸送物性、U.S. Standard Atmosphere 1976 を実装済み。
- 航空慣用単位の明示的変換、等エントロピー流れ、`FlightCondition` を実装済み。
- 出典、仮定、適用範囲、API reference、実行可能例を Sphinx 文書として整備済み。
- lint、型検査、branch coverage 95% 以上、doctest、wheel/sdist build、隔離環境への wheel 導入を検証済み。
- GitHub への push、PyPI への公開は別作業とする。

## 0.2 の実装状況

- 垂直衝撃波、斜め衝撃波、theta–beta–Mach 関係、弱解・強解、
  Prandtl–Meyer 膨張、超音速ピトー管関係を実装済み。
- 角度は radian、状態量比は下流／上流、衝撃波の全圧比は下流全圧／
  上流全圧で統一した。
- 斜め衝撃波の非付着条件は `NoAttachedShockError` とし、垂直衝撃波への
  暗黙の切り替えは行わない。
- 逆問題は SciPy の有界数値解法を内部の固定高精度許容誤差で使用する。
- スカラーと NumPy 配列、ブロードキャスト、凍結 dataclass の結果型、
  NACA Report 1135 の代表値をテスト済み。

## 0.1 の設計決定

- Python 3.12 以上を対象とし、MIT License で公開する。
- ランタイム依存関係は NumPy と SciPy とする。開発・依存管理には uv、ビルドには Hatchling を使用する。
- 公開計算 API は SI 単位の数値を受け取る。航空慣用単位は `units` の明示的な変換関数で扱い、単位付きオブジェクトは導入しない。
- スカラー入力には `float`、配列相当の入力にはブロードキャスト後の形状を持つ `numpy.ndarray` を返し、計算と出力は `float64` に統一する。
- 複数の状態量は、名前付きフィールドを持つ凍結 dataclass で返す。
- 数学的または物理的に不正な入力は `ValueError`、モデルの対応範囲外は `ModelRangeError`、計算可能な外挿は `ApplicabilityWarning` で通知する。
- 配列の一部に不正値がある場合も呼び出し全体を失敗させる。NaN と無限大は入力として受け付けない。
- 完全気体の熱力学モデルと粘性・熱伝導率の輸送物性モデルを分離する。
- 標準大気は U.S. Standard Atmosphere 1976 を基準とし、幾何高度 -5 km から 86 km を扱う。
- 等エントロピー関係の状態量比は全量／静量で統一し、逆計算の数値解法は内部で高精度に固定する。
- `FlightCondition` は幾何高度と Mach 数または速度から生成し、代表長さは任意入力とする。「有効動圧」という曖昧な名称は公開 API に含めない。
- 公開 API と docstring は英語で記述し、日本語の概要を併記する。Sphinx、numpydoc、doctest で文書を検証する。
- pytest、ruff、mypy を使用し、branch coverage 95% 以上を 0.1 の完了条件とする。
- 0.x でも Semantic Versioning を使用し、可能な限り 1 minor version の非推奨期間を設ける。

## 0.2 の設計決定

- 垂直衝撃波、斜め衝撃波、theta–beta–Mach 関係、弱解・強解、Prandtl–Meyer 膨張、超音速ピトー管関係を対象とする。
- 角度の入力と出力は radian に固定し、degree との変換は `units` の明示的な変換関数で行う。
- 複数の状態量は、名前付きフィールドを持つ凍結 dataclass で返す。
- 斜め衝撃波に付着解がない場合は、暗黙に垂直衝撃波へ切り替えず、`NoAttachedShockError` を送出する。
- 数式と基準値は NASA Report 1135 を中心に選定する。
- 数値解法の許容誤差は実装内部で固定し、利用者向け引数にはしない。

## 将来リリースの方針

### 0.3: 平板境界層

- 滑面かつゼロ圧力勾配の平板について、層流、乱流、指定遷移、圧縮性補正を対象とする。
- 表面粗さと剥離推定は、適切な粗さモデルおよび圧力勾配モデルを設計する後続リリースへ延期する。
- 乱流相関は 1/5 乗則と Schlichting 型を明示的に選択可能とし、既定値は Schlichting 型とする。
- 遷移計算では利用者が遷移 Reynolds 数を明示し、既定の遷移 Reynolds 数は設けない。
- 圧縮性補正として Eckert 基準温度法と Van Driest II の両方を実装する。
- 遷移を含む混合計算では、層流部へ Eckert 基準温度法、乱流部へ Van Driest II を適用できるようにする。
- 壁温が指定されていない場合は断熱壁を仮定する。
- 結果には境界層厚さ、排除厚さ、運動量厚さ、局所・平均摩擦係数、壁面せん断応力、単位幅当たりの摩擦抗力を含める。
- 詳細な相関式、適用 Reynolds 数範囲、検証文献、許容誤差は 0.3 の着手時に確定する。
